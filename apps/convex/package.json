{
  "name": "@lilbuddy/convex",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Convex backend — real-time cloud database for session state",
  "scripts": {
    "dev": "convex dev",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "convex": "^1.21.0"
  },
  "devDependencies": {
    "convex-test": "^0.0.35",
    "@vitest/coverage-v8": "^3.2.1",
    "typescript": "^5.7.2",
    "vitest": "^3.2.1"
  }
}
